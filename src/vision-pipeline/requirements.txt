# ── Core ──────────────────────────────────────────────────────────────────────
numpy>=1.24.0
opencv-python-headless>=4.8.0   # headless: без GUI-зависимостей внутри контейнера

# ── ML / Detection ────────────────────────────────────────────────────────────
# torch и torchvision устанавливаются в Dockerfile с CPU-индекса PyTorch
# ultralytics = YOLOv8 / YOLO11
ultralytics>=8.0.0

# ── SAM / SAM2 зависимости ────────────────────────────────────────────────────
# SAM1 (опционально, если нужна цепочка SAM→SAM2)
segment-anything>=1.0
# SAM2 устанавливается из GitHub в Dockerfile:
#   pip install git+https://github.com/facebookresearch/sam2.git@main
# Явные зависимости SAM2:
hydra-core>=1.3.2
iopath>=0.1.10

# ── Цветовой анализ ───────────────────────────────────────────────────────────
scikit-learn>=1.3.0
Pillow>=10.0.0

# ── Кодирование масок ─────────────────────────────────────────────────────────
pycocotools>=2.0.7

# ── Транспорт (NATS) ──────────────────────────────────────────────────────────
nats-py>=2.6.0
