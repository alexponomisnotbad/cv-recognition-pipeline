# ── Core ──────────────────────────────────────────────────────────────────────
numpy>=1.24.0
opencv-python-headless>=4.8.0   # headless: без GUI-зависимостей внутри контейнера

# ── ML / Detection ────────────────────────────────────────────────────────────
# torch и torchvision устанавливаются в Dockerfile с CPU-индекса PyTorch
# ultralytics = YOLOv8 / YOLO11
ultralytics>=8.0.0
onnx>=1.16.0onnxruntime>=1.17.0  # для инференса ONNX моделей (ResNet классификатор)
# ── Цветовой анализ ───────────────────────────────────────────────────────────
scikit-learn>=1.3.0
Pillow>=10.0.0

# ── Кодирование масок ─────────────────────────────────────────────────────────
pycocotools>=2.0.7

# ── Транспорт (NATS) ──────────────────────────────────────────────────────────
nats-py>=2.6.0

# ── Таблицы / БД ──────────────────────────────────────────────────────────────
pandas>=2.2.0
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
pymysql>=1.1.1
oracledb>=2.4.1
